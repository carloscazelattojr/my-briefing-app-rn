import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        marginTop: 120,
    },
    image: {
        width: 150,
        height: 150,
        borderRadius: 80,
        marginBottom: 10,
    },
    title: {
        fontSize: 30,
        fontWeight: 'bold',
        marginTop: 30,
    },
    subtitle: {
        color: '#333'
    },
});